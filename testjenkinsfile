pipeline{
    agent any

    stages {

        stage('Dep install'){
            agent{ docker { image 'nodejs'} }

            steps{
                sh 'npm cache clean'
                sh 'npm install'
            }
        }
    }
}